const EMAIL_TAKEN = 'Email already in use';
const USERNAME_TAKEN = 'Username already in use';
const GENERIC_ERROR = 'Something went wrong';
const NOT_LOGGED_IN = 'Not logged in';
const BAD_CREDENTIALS = 'Bad email or password, please check your credentials';
const PAYLOAD_COOKIE = 'payload';
const SIGNATURE_COOKIE = 'signature';

export {
  EMAIL_TAKEN,
  USERNAME_TAKEN,
  GENERIC_ERROR,
  NOT_LOGGED_IN,
  BAD_CREDENTIALS,
  PAYLOAD_COOKIE,
  SIGNATURE_COOKIE,
};
