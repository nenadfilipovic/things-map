"Provided data for sign up."
input SignUpInput {
  "User email."
  email: String!
  "User first name."
  firstName: String!
  "User last name."
  lastName: String!
  "User password."
  password: String!
  "User username."
  username: String!
}

"Sign up result."
type SignUpResult {
  "Errors."
  errors: [Error]
  "Message."
  message: String
}

extend type Mutation {
  "Sign up."
  signUp("Input data." input: SignUpInput!): SignUpResult!
}
