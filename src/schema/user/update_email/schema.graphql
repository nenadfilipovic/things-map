"Provided data for update email."
input UpdateEmailInput {
  "Email."
  email: String!
}

"Update email result."
type UpdateEmailResult {
  "Errors."
  errors: [Error]
  "Message."
  message: String
}

extend type Mutation {
  "Update email."
  updateEmail("Input data." input: UpdateEmailInput!): UpdateEmailResult!
}
